package com.lenovo.greendao.gen;

import java.util.List;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.query.Query;
import org.greenrobot.greendao.query.QueryBuilder;

import com.lenovo.common.entity.JoinImageLabelEntity;

import com.lenovo.common.entity.ImageEntity;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "IMAGE_ENTITY".
*/
public class ImageEntityDao extends AbstractDao<ImageEntity, Long> {

    public static final String TABLENAME = "IMAGE_ENTITY";

    /**
     * Properties of entity ImageEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Name = new Property(1, String.class, "name", false, "NAME");
        public final static Property Path = new Property(2, String.class, "path", false, "PATH");
        public final static Property Created = new Property(3, long.class, "created", false, "CREATED");
        public final static Property Updated = new Property(4, long.class, "updated", false, "UPDATED");
        public final static Property Size = new Property(5, long.class, "size", false, "SIZE");
        public final static Property UniqueString = new Property(6, String.class, "uniqueString", false, "UNIQUE_STRING");
    };

    private DaoSession daoSession;

    private Query<ImageEntity> labelEntity_ImageEntityListQuery;

    public ImageEntityDao(DaoConfig config) {
        super(config);
    }
    
    public ImageEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"IMAGE_ENTITY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"NAME\" TEXT," + // 1: name
                "\"PATH\" TEXT," + // 2: path
                "\"CREATED\" INTEGER NOT NULL ," + // 3: created
                "\"UPDATED\" INTEGER NOT NULL ," + // 4: updated
                "\"SIZE\" INTEGER NOT NULL ," + // 5: size
                "\"UNIQUE_STRING\" TEXT UNIQUE );"); // 6: uniqueString
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"IMAGE_ENTITY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ImageEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
 
        String path = entity.getPath();
        if (path != null) {
            stmt.bindString(3, path);
        }
        stmt.bindLong(4, entity.getCreated());
        stmt.bindLong(5, entity.getUpdated());
        stmt.bindLong(6, entity.getSize());
 
        String uniqueString = entity.getUniqueString();
        if (uniqueString != null) {
            stmt.bindString(7, uniqueString);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ImageEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
 
        String path = entity.getPath();
        if (path != null) {
            stmt.bindString(3, path);
        }
        stmt.bindLong(4, entity.getCreated());
        stmt.bindLong(5, entity.getUpdated());
        stmt.bindLong(6, entity.getSize());
 
        String uniqueString = entity.getUniqueString();
        if (uniqueString != null) {
            stmt.bindString(7, uniqueString);
        }
    }

    @Override
    protected final void attachEntity(ImageEntity entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ImageEntity readEntity(Cursor cursor, int offset) {
        ImageEntity entity = new ImageEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // name
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // path
            cursor.getLong(offset + 3), // created
            cursor.getLong(offset + 4), // updated
            cursor.getLong(offset + 5), // size
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6) // uniqueString
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ImageEntity entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setPath(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setCreated(cursor.getLong(offset + 3));
        entity.setUpdated(cursor.getLong(offset + 4));
        entity.setSize(cursor.getLong(offset + 5));
        entity.setUniqueString(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ImageEntity entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ImageEntity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "imageEntityList" to-many relationship of LabelEntity. */
    public List<ImageEntity> _queryLabelEntity_ImageEntityList(Long labelId) {
        synchronized (this) {
            if (labelEntity_ImageEntityListQuery == null) {
                QueryBuilder<ImageEntity> queryBuilder = queryBuilder();
                queryBuilder.join(JoinImageLabelEntity.class, JoinImageLabelEntityDao.Properties.ImageId)
                    .where(JoinImageLabelEntityDao.Properties.LabelId.eq(labelId));
                labelEntity_ImageEntityListQuery = queryBuilder.build();
            }
        }
        Query<ImageEntity> query = labelEntity_ImageEntityListQuery.forCurrentThread();
        query.setParameter(0, labelId);
        return query.list();
    }

}
